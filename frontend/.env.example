# Copy relevant lines into .env.local for local development (never commit .env.local)

# --- Vercel / production (default in .env, no override needed) ---
# VITE_API_BASE_URL=/api

# --- Local development (FastAPI running on port 8000) ---
# VITE_API_BASE_URL=http://localhost:8000/api
